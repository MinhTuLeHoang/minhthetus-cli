---
description: Automatically updates the technical guides based on the latest shell script implementations.
---

1.  Read the `update-docs` skill instructions in `.agents/skills/update-docs/SKILL.md`.
2.  List all scripts in `src/scripts/`.
3.  List all existing guides in `guide/cli-functions/`.
4.  For each script, compare its local implementation with the corresponding documentation.
5.  Apply necessary updates to existing documentation or create new documentation files.
6.  Synchronize any configuration changes within `~/.minhthetus-cli/` to `guide/folder-structure-config.md`.
7.  Summarize the updates made to the repository.
